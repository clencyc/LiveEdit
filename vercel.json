{
  "buildCommand": "cd LiveEditFronten && npm install && npm run build",
  "outputDirectory": "LiveEditFronten/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
